{
  "name": "cypress-poc",
  "version": "1.0.0",
  "description": "MooveIT Automated Test POC",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint cypress",
    "cypress:run": "npx cypress run",
    "cypress:run:chrome": "npx cypress run --browser chrome",
    "cypress:run:e2e:chrome": "npx cypress run --browser chrome --spec 'cypress/e2e/*.cy.js'"
  },
  "author": "Leandro Cuevas - Roman Diaz",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.3.1"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.28.0",
    "cypress-dotenv": "^2.0.0",
    "cypress-xpath": "^2.0.1",
    "dotenv": "^16.0.1",
    "eslint": "^8.20.0"
  }
}
